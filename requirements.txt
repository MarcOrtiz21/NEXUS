# Core
yfinance>=0.2.30
pandas>=2.0
numpy>=1.24
requests>=2.28

# CLI
rich>=13.0

# Desktop UI (macOS-like cards + trackpad scroll)
customtkinter>=5.2
pillow>=10.0

# macOS real window blur (CGS / Quartz); ignored on other platforms
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# News feed
feedparser>=6.0

# Web dashboard (opcional)
fastapi>=0.100
uvicorn>=0.23

# NLP (opcional, solo si use_finbert=True)
# transformers>=4.30
# torch>=2.0
